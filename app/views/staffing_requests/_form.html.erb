<%= form_for(@staffing_request) do |f| %>
  <% if @staffing_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staffing_request.errors.count, "error") %> prohibited this staffing_request from being saved:</h2>

      <ul>
      <% @staffing_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
